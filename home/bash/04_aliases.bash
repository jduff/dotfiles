#!/bin/bash

# git
alias gs='git status'
alias gc='git commit -m'
alias ga='git add'

# ruby
alias bx='bundle exec'
alias br='bin/rails'

# unity
alias unity='open -na Unity --args -force-metal'

alias ls='ls -G'
